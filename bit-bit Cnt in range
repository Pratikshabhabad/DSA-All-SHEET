class Solution {
    public int[] countBits(int n) {
        int ans[]=new int[n+1];

        for(int i=0;i<=n;i++)
        {
            int num=i;

           int bit=Integer.bitCount(num);
           ans[i]=bit;

        }

        return ans;
    }
}
